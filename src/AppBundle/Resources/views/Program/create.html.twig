{# src/AppBundle/Resources/views/Program/create.html.twig #}
{% extends 'AppBundle::layout.html.twig' %}
{% form_theme form 'form/fields.html.twig' %}
{% block title %} {{'program.create'|trans({}, 'messages')}} {% endblock %}
{% block stylesheets %}
      <link href="{{ asset("bundles/app/js/bootstrap-colorpicker/css/colorpicker.css")}}" rel="stylesheet" />      
{% endblock %} 
{% block page %}


</script>

   <ol class='breadcrumb'><li>{{'katalog'|trans({}, 'messages')}}</li><li><a href="{{ path('program_index') }}">{{'program.programs'|trans({}, 'messages')}}</a></li><li>{{'program.create'|trans({}, 'messages')}}</li></ol></ol>   
    <div class="col-lg-12">
        
        <div class="panel">  
          <header class="panel-heading">{{'program.create'|trans({}, 'messages')}}</header>
            <div class="panel-body">     
                <!--messages -->
        
        
         {% for flashMessage in app.session.flashbag.get('notice') %}
            <div class="alert alert-warning">
                {{ flashMessage }}
            </div>
         {% endfor %}

        {% for flashMessage in app.session.flashbag.get('error') %}
            <div class="alert alert-warning">
                {{ flashMessage }}
            </div>
         {% endfor %}
           
                  <div class="panel-body">
                     <div class="position-center">   
 
            {{ form(form) }}
              
       </div> 
      

        
            
        <div>
            <a href="{{ path('program_index') }}">{{'go_back'|trans({}, 'messages')}}</a>
        </div>
</div>
{% endblock %}

                                
{%block javascript %}    
    <script src="{{ asset("bundles/app/js/bootstrap-colorpicker/js/bootstrap-colorpicker.js")}}"></script>
    <script type="text/javascript">
        $('.colorpicker-default').colorpicker();
    </script>
{% endblock %}